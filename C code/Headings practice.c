// Write ANSI C function headings for the functions described below:
// donut() takes an int argument and prints that number of 0's.
void dounut(int num)

// gear() takes two int arguments and returns type int.
int gear(int a, int b)

// guess() takes no arguments and returns an int value.
int guess(void)

// stuff_it() takes a double and the address of a double 
// variable and stores the first value in the given location

void stuff_it(double num, double* pNumAddy)

// n_to_char() takes an int argument and returns a char.
char n_to_char(int num)

// digits() takes a double argument and 
// an int argument and returns an int.
int digits(double a, int b)

// which() takes two addresses of double as 
// arguments and returns the address of a double.
double* which(double* px, double* py)

// random() takes no argument and returns an int.
int random(void)
